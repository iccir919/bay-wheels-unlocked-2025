# Database Target (supabase or local)
DB_TARGET=local

# Local PostgreSQL Configuration (for DB_TARGET=local)
LOCAL_POSTGRES_URL=postgres://user:password@localhost:5432/bikeshare_local

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS (optional - for production)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000